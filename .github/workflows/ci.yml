name: Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to release (e.g., v1.0.0)'
        required: true
        type: string

permissions:
  contents: write
  pull-requests: write

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: Use Node.js 20.x
      uses: actions/setup-node@v4
      with:
        node-version: 20.x
        
    - name: Install dependencies
      run: npm install
      
    - name: Run tests
      run: npm test

  create-release:
    runs-on: ubuntu-latest
    needs: test
    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0
        token: ${{ secrets.GITHUB_TOKEN }}
    
    - name: Use Node.js 20.x
      uses: actions/setup-node@v4
      with:
        node-version: 20.x
    
    - name: Install dependencies
      run: npm install
    
    - name: Configure git
      run: |
        git config --global user.email "action@github.com"
        git config --global user.name "GitHub Action"
    
    - name: Create release with release-it
      run: |
        version="${{ inputs.version }}"
        # Remove 'v' prefix if present for release-it
        clean_version=${version#v}
        echo "Creating release for version: $clean_version"
        
        # Use release-it to handle version bump, commit, tag, and changelog
        npx release-it $clean_version --ci --no-npm

  deploy-staging:
    runs-on: ubuntu-latest
    needs: create-release
    environment: staging
    steps:
    - uses: actions/checkout@v4
    
    - name: Use Node.js 20.x
      uses: actions/setup-node@v4
      with:
        node-version: 20.x
        
    - name: Install dependencies
      run: npm install
      
    - name: Deploy to Staging
      run: |
        echo "Deploying version ${{ inputs.version }} to staging environment..."
        echo "Application deployed to staging"

  deploy-production:
    runs-on: ubuntu-latest
    needs: deploy-staging
    environment: production
    steps:
    - uses: actions/checkout@v4
    
    - name: Use Node.js 20.x
      uses: actions/setup-node@v4
      with:
        node-version: 20.x
        
    - name: Install dependencies
      run: npm install
      
    - name: Deploy to Production
      run: |
        echo "Deploying version ${{ inputs.version }} to production environment..."
        echo "Application deployed to production"